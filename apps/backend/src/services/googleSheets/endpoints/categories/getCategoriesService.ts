import { setCredentials, ensureCategoriesSchema, find, update } from '../../';

export const getCategoriesService = {
  setCredentials,
  ensureCategoriesSchema,
  find,
  update,
};

export default getCategoriesService;

import { setCredentials, ensureTableExists, find, update } from '../../';

export const updateIncomeService = {
  setCredentials,
  ensureTableExists,
  find,
  update,
};

export default updateIncomeService;

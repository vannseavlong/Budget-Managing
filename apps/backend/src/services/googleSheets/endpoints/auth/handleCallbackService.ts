import {
  getTokens,
  setCredentials,
  getUserInfo,
  getOrCreateUserDatabase,
  find,
} from '../../';

export const handleCallbackService = {
  getTokens,
  setCredentials,
  getUserInfo,
  getOrCreateUserDatabase,
  find,
};

export default handleCallbackService;
